[project]
name = "dbx-models"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "databricks-sdk",
    "mlflow",
]


[project.optional-dependencies]
dev = [
    "ruff",
    "nbstripout",
]
